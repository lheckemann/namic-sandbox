#
# The path of the input folder for images.
# All images are assumed to be in the same folder.
# (don't forget to have backslash as the last character)
#
# If images are in different folders ignore
# this parameter and supply the full pathname
# as filename.
#
-i @MultiImageRegistration_TEST_OUTPUT_DIR@/BsplineImages_10/Images/


# 
# The path of the output folder.
# All outputs are saved to this folder
#
#
-o @MultiImageRegistration_TEST_OUTPUT_DIR@/Registration004/      
 
 
# names of the input files
# if inputFolder is specified, the pathname is relative to that 
# folder. Otherwise supply the full pathname
#
 -f  0.mhd
 -f  1.mhd
 -f  2.mhd
 -f  3.mhd
 -f  4.mhd
 -f  5.mhd
 -f  6.mhd
 -f  7.mhd
 -f  8.mhd
 -f  9.mhd

 
