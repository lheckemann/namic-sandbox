PROJECT(PointLandmarkDetection)

FIND_PACKAGE(ITK REQUIRED)
INCLUDE(${ITK_USE_FILE})
INCLUDE(Dart)
ENABLE_TESTING()


SET(EXECUTABLE_OUTPUT_PATH "${CMAKE_CURRENT_BINARY_DIR}/bin" CACHE INTERNAL "")

SUBDIRS(Code)
SUBDIRS(Tests)


