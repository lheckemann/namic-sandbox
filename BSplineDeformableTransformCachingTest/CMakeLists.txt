PROJECT(MattesMetricCachingTest)

FIND_PACKAGE(ITK REQUIRED)
IF(ITK_FOUND)
  INCLUDE(${ITK_USE_FILE})
ENDIF(ITK_FOUND)

ENABLE_TESTING()

SET( SIZE_LIST 32 50 70 80 90 100 110 )
#SET( SIZE_LIST 256 )
SET(NUMBER_OF_CALLS 1)

SUBDIRS(
  Source
  Testing
  Documents
  )
