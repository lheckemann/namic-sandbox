find_package(ITK REQUIRED)
if(ITK_FOUND)
  include(${ITK_USE_FILE})
else(ITK_FOUND)
  message(FATAL_ERROR "Cannot build without ITK. Please set ITK_DIR.")
endif(ITK_FOUND)

include_directories(${MultiImageRegistration_INCLUDE_DIRS})
link_directories(${MultiImageRegistration_LIBRARY_DIRS})

