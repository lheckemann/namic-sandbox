PROJECT(WFEngine)

SET(TEST_SOURCE_DIRS)

OPTION(WFOPTIONSDIALOG "Want you to build the WFOptionsDialog?")
IF(WFOPTIONSDIALOG)
  SET(TEST_SOURCE_DIRS ${TEST_SOURCE_DIRS} WFOptionsDialog)
ENDIF(WFOPTIONSDIALOG)

OPTION(WFTESTCLIENT "Want you to build the WFTestClient?")
IF(WFTESTCLIENT)
  SET(TEST_SOURCE_DIRS ${TEST_SOURCE_DIRS} WFTestClient)
ENDIF(WFTESTCLIENT)

OPTION(WFDYNAMICCLIENT "Want you to build the WFDynamicClient?")
IF(WFDYNAMICCLIENT)
  SET(TEST_SOURCE_DIRS ${TEST_SOURCE_DIRS} WFDynamicClient)
ENDIF(WFDYNAMICCLIENT)


SUBDIRS(${TEST_SOURCE_DIRS})
INCLUDE_DIRECTORIES(${TEST_SOURCE_DIRS})
